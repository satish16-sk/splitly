// Pure logic placeholder to compute simplified debt splits
export const simplifyDebts = (transactions) => {
  // transactions contains: from, to, amount
  // Return simplified transaction list
  return transactions;
};
